MACHINE_ID=1

# ── Development mode ──────────────────────────────────────────────────────────
# Set to true when running electron:dev / npm run dev.
# Disables the Loki sink entirely — no network calls and no local SQLite buffer.
DEV_MODE=false
